# HTTP/2 Bomb MCP — End-to-End Test Report

**Date:** 2026-06-05 (CEST)  
**Agent host:** 192.168.2.20 / 192.168.2.155 on `192.168.2.0/24`  
**Plugin:** `/home/danii/.cursor/plugins/local/http2-bomb-mcp/`

## 1. Connectivity discovery

| Target | Method | Result |
|--------|--------|--------|
| ai-workstation VM 200 `danii@192.168.2.116` | SSH BatchMode | **FAIL** — `No route to host` |
| Proxmox `root@192.168.2.1` / `danii@192.168.2.1` | SSH | **FAIL** — connection refused (not an SSH endpoint from this LAN) |
| LAN gateway `192.168.2.1` | ping | OK |
| `192.168.2.115` | ping, nmap | Host up; **22/tcp open** (SSH, pubkey required); **80/443 closed** |
| VM 101 `192.168.2.101` | ping (later), nmap | Intermittent; nmap **host up**, **no open** ports on 22,80,443,3389,5985 |
| Plugin configs | grep | Only lab remote: `danii@192.168.2.116` — **no Win11 IP** documented |
| `~/.ssh/config` | read | No Proxmox/station-2 host entry |

**Conclusion:** VM 200 (ai-workstation) appears **down or off-LAN**. Cannot run remote nginx/httpd benchmarks. Cannot run `qm shutdown 200` without Proxmox SSH/API credentials.

## 2. CLI + tunnel (local)

| Command | Exit | Notes |
|---------|------|-------|
| `./bin/http2-bomb --help` | 0 | OK |
| `./bin/http2-bomb variants` | 0 | nginx, httpd, envoy, pingora, microsoft-iis listed |
| `./bin/http2-bomb tunnel show` | 0 | `mode=none`, direct |
| `./bin/http2-bomb tunnel test --host 127.0.0.1` | 0 | `ok: false`, errno 111 (no local :443 listener) |
| `./bin/http2-bomb tunnel test --host 1.1.1.1 --port 443` | 0 | `ok: true`, `latency_ms: 12.4` |
| `py_compile` cli + tunnel modules | 0 | OK |
| `./bin/http2-bomb probe --host 127.0.0.1 --port 8443` | 0 | PROBE FAILED (no lab on this machine) |
| `./bin/http2-bomb mcp-info` | 0 | MCP JSON snippet printed |

## 3. MCP smoke

```text
.venv/bin/python3 -c "import http2_bomb_mcp; print('mcp ok')"
```

- **Exit 0** — `mcp ok`
- Exported tools include: `probe_http2`, `run_http2_bomb_test`, `run_http2_bomb_benchmark`, `list_http2_bomb_variants`, `configure_http2_bomb_tunnel`

## 4. Benchmark harness (ai-workstation)

**Skipped** — SSH to `192.168.2.116` unreachable.

Historical metrics (2026-06-04, same plugin logs): nginx `apex_scaled` **20/20** bomb_ok, **40.06** wire_mb; httpd `apex_cookie_scaled` **12/12**, **0.19** wire_mb.

## 5. Windows 11 VM 101 — IIS `apex_iis_mp`

**Not executed on Windows** — no RDP/WinRM/SSH to Win11 from agent; IIS not listening on discovered LAN hosts.

### Linux stub (from agent)

```bash
cd .../benchmark
python3 benchmark_runner.py --host 192.168.2.101 --variant iis --mode apex_iis_mp --iis-preset 8gb --connections 2
```

| Metric | Value |
|--------|-------|
| run_id | a682da5a |
| exit | 0 |
| connections_bomb_ok | **0** |
| wire_mb | **0.0** |
| server_down | true |
| connections_requested | 10000 (preset-driven; `--connections` ignored for `apex_iis_mp`) |
| notes | Linux stub — orchestrator required on Windows |

### Run on Win11 (PowerShell, after IIS + HTTPS binding)

1. Install **IIS** + **HTTP/2** (Windows 11: optional features → IIS; bind site on **443** with cert).
2. Copy plugin tree or sync `benchmark/` + `vendor/califio-publications/.../microsoft-iis/poc/`.
3. Allow **443** in Windows Firewall (inbound).
4. From attack VM or same host (lab only):

```powershell
# Verify listener
Test-NetConnection -ComputerName 127.0.0.1 -Port 443
curl.exe -k -I https://127.0.0.1/

# Orchestrator (5 processes for 8gb preset)
powershell -ExecutionPolicy Bypass -File "C:\path\to\http2-bomb-mcp\benchmark\iis_apex_orchestrator.ps1" -Host 127.0.0.1 -Port 443 -Preset 8gb
```

5. From Linux when Win11 IP is known (e.g. `192.168.2.101`):

```bash
python3 benchmark_runner.py --host <WIN11_IP> --variant iis --mode apex_iis_mp --iis-preset 8gb
```

(On Linux this remains a stub unless you run the orchestrator on Windows.)

## 6. Bugs / observations

- **Not a bug:** `apex_iis_mp` ignores `--connections`; uses IIS preset totals (`iis_apex_runner.py`).
- **Environment:** Local docker `ps` hung >30s — no local nginx/httpd lab confirmed on agent host.
- **VM 101:** User just started Win11 — allow time for boot; confirm IP in Proxmox UI (`qm guest cmd 101 network-get-interfaces` on Proxmox host).

## 7. Recommended next steps

1. **Free RAM for Win11:** On Proxmox `station-2` (when you have shell): `qm shutdown 200` if ai-workstation is still running and OOMing the node.
2. **Bring back Linux lab (optional):** Start VM 200 or run `lab-replay/deploy_proxmox.sh` once `192.168.2.116` is reachable; re-run small tests (`--connections 5`).
3. **Win11 IIS:** Install IIS, HTTPS :443, firewall rule; note guest IP; run `iis_apex_orchestrator.ps1` locally first.
4. **Document IP:** Add `WIN11_LAB_HOST` to plugin lab notes when IP is stable (not in repo today).
5. **Re-run this E2E** after 116 and/or 101 are reachable.

## 8. Summary

| Component | Status |
|-----------|--------|
| CLI / tunnel / compile | **PASS** |
| MCP import | **PASS** |
| Remote nginx/httpd benchmark | **BLOCKED** (VM 200 down) |
| IIS apex_iis_mp live | **BLOCKED** (no IIS listener / VM access) |
| Proxmox automation | **BLOCKED** (no credentials) |

### Background ping sweep (task 376708, completed)

Hosts responding to ping on 192.168.2.0/24: **.1, .20, .25, .35, .37, .115, .155, .193, .198** (`.101` not in list at scan time).

nmap `192.168.2.101`: host up, **no open** ports on 22,80,443,3389,5985.
