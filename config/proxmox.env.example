# Proxmox lab target (ai-workstation VM 200)
# Copy to proxmox.env and adjust if needed.

REMOTE=danii@192.168.2.116
REMOTE_DIR=~/APEX-Ngin2dos
MEM=8g
PORT=8443

# nginx lab listens on REMOTE at 127.0.0.1:8443 (Docker inside ai-workstation).
# Run deploy from your laptop — attacks execute ON the remote, not locally.
#
#   source config/proxmox.env   # optional
#   ./lab-replay/deploy_proxmox.sh smoke
#   ./lab-replay/deploy_proxmox.sh campaign
