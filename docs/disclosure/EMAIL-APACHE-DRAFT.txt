Subject: CVE-2026-49975 incomplete fix — mod_http2 2.0.41 fat-cookie DoS on httpd 2.4.68

To: security@apache.org

Hello Apache Security Team,

I am reporting a denial-of-service condition in Apache httpd mod_http2 that remains
exploitable on DEFAULT configurations after httpd 2.4.68 / mod_http2 v2.0.41,
which your advisory lists as fixing CVE-2026-49975.

SUMMARY
-------
I independently validated post-patch behavior using the APEX-Ngin2dos harness.

1. EMPTY-COOKIE path (original CVE-2026-49975 PoC): MITIGATED on 2.4.68
   - 44 connections, 1.7 MB wire, flat RSS, probe OK

2. FAT-COOKIE indexed-reference chain (within LimitRequestFields): NOT MITIGATED
   - 100 connections, 10 MB wire → oom_likely + server_down
   - 500+ connections → multi-GiB RSS (prior runs)

The 2.0.41 fix (commit 47d3100b25) addresses empty-crumb accounting but not
aggregate memory retention on the cookie-merge path (apr_psprintf per merge).

I request CVE evaluation as incomplete remediation of CVE-2026-49975.

ATTACHMENTS
-----------
- VENDOR-REPORT-APACHE-INCOMPLETE-FIX.md (full technical report)
- cve_evidence_*.tar.gz from ./scripts/collect-cve-evidence.sh

REPRODUCTION
------------
git clone https://github.com/Leviticus-Triage/APEX-Ngin2dos.git
./lab-replay-httpd/replay.sh build && ./lab-replay-httpd/replay.sh start 8g
./scripts/collect-cve-evidence.sh --apache-threshold

I offer a 90-day coordinated disclosure embargo and can assist with patch validation.

Best regards,
Daniel F. Hensen
[email]
[PGP fingerprint if available]
