# Benchmark logs

Runtime CSV/JSONL output is written here during local runs.

**Committed samples only:** see `samples/*.example` for the expected format.

Local run artifacts (not tracked in git):

- `benchmark_results.csv`
- `benchmark_runs.jsonl`

Copy a sample to bootstrap headers:

```bash
cp samples/benchmark_results.csv.example benchmark_results.csv
```
